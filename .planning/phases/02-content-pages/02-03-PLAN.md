---
phase: 02-content-pages
plan: "03"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/pages/about.astro
  - src/pages/events.astro
  - src/pages/join.astro
autonomous: true
requirements: [ABUT-01, EVNT-01, CONT-01, CONT-02, CONT-03]

user_setup:
  - service: Web3Forms
    why: "Contact form email forwarding — CONT-02 requires submissions arrive in group inbox"
    env_vars: []
    account_setup:
      - task: "Register at https://web3forms.com/#start with the group email address"
        result: "Receive a public access key via email (immediate)"
    dashboard_config:
      - task: "Enable hCaptcha for the form"
        location: "app.web3forms.com > select form > Spam Protection > Enable hCaptcha"

must_haves:
  truths:
    - "User can navigate to /about and read about the Cooroy Scout Group's background and scouting"
    - "User can navigate to /events and see at least one term program entry (not placeholder)"
    - "User can navigate to /join, fill in name, email, and child's age, and submit the form"
    - "Form submissions are forwarded to the group email via Web3Forms"
    - "Form includes hCaptcha spam protection that blocks bot submissions"
  artifacts:
    - path: "src/pages/about.astro"
      provides: "About page at /about with group background"
      min_lines: 30
      contains: "BaseLayout"
    - path: "src/pages/events.astro"
      provides: "Events/term program page at /events"
      min_lines: 40
      contains: "termProgram"
    - path: "src/pages/join.astro"
      provides: "Contact/join page at /join with Web3Forms form"
      min_lines: 80
      contains: "web3forms"
  key_links:
    - from: "src/pages/join.astro"
      to: "https://api.web3forms.com/submit"
      via: "fetch POST in client-side script"
      pattern: "api.web3forms.com"
    - from: "src/pages/join.astro"
      to: "hCaptcha widget"
      via: "Web3Forms client script + data-captcha div"
      pattern: "h-captcha"
    - from: "src/pages/events.astro"
      to: "hardcoded termProgram array"
      via: "Astro frontmatter data → template rendering"
      pattern: "termProgram"
---

<objective>
Create the About page, Events/Term Program page, and Contact/Join page with Web3Forms integration and hCaptcha spam protection.

Purpose: Parents need to learn about the group (ABUT-01), see upcoming activities (EVNT-01), and submit an inquiry to join (CONT-01, CONT-02, CONT-03). The contact form is the primary conversion action for the site.

Output: Three new pages — about.astro, events.astro, join.astro — completing all non-section content requirements.
</objective>

<execution_context>
@C:/Users/DCampbell/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/DCampbell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-pages/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — brand tokens available when this plan executes -->

From src/styles/global.css (@theme tokens):
```css
--color-scout-blue:       #2EA3F2;
--color-scout-navy:       #212C66;
--color-scout-gold:       #F0CB37;
--font-display: "Montserrat", "Arial Black", sans-serif;
--font-body:    "Nunito", "Arial", sans-serif;
```

From src/layouts/BaseLayout.astro:
```typescript
interface Props {
  title: string;
  description?: string;
}
// Renders: Nav + <slot /> + Footer
```

From src/components/Nav.astro:
- Links to: /, /sections, /about, /events, /gallery, /join
- "Join Us" CTA button links to /join
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create About page and Events/Term Program page</name>
  <files>src/pages/about.astro, src/pages/events.astro</files>
  <action>
**Create `src/pages/about.astro`** (ABUT-01):

Import BaseLayout. Title: "About Us".

Page sections:
1. **Hero** — `bg-scout-blue text-white py-16 md:py-20` with heading "About Cooroy Scouts" and subtext "Part of Scouts Queensland since [year TBD] — building skills, friendships, and a love of the outdoors in the Noosa hinterland"
2. **Our Story** — `bg-white py-16` with `max-w-3xl mx-auto px-4 prose` content:
   - Paragraph about Cooroy Scout Group being part of Scouts Queensland, located in the Noosa hinterland
   - Paragraph about what Scouting offers: outdoor adventure, life skills, teamwork, community service
   - Paragraph about the group welcoming all young people aged 5-17, no experience needed
   - Note: Use real-sounding content that accurately describes a typical Queensland Scout group. The group leader can refine specifics later.
3. **What is Scouting?** — `bg-gray-50 py-16` section explaining the Scouts method:
   - Brief explanation of the Scout method: learning by doing, small teams, outdoor-focused
   - Mention the 4 sections (Joeys, Cubs, Scouts, Venturers) with link to /sections
   - Mention the Scout Promise and Law as guiding principles
4. **Our Values** — `bg-white py-16` with a 3-column grid (1 col on mobile) showing 3 core values:
   - Adventure: "We learn by doing — camping, hiking, and exploring the outdoors"
   - Community: "We serve our local community and look after the environment"
   - Growth: "We develop leadership, resilience, and skills for life"
   Each value as a card with `text-scout-navy font-display` heading
5. **CTA** — "Ready to join the adventure?" with gold button linking to /join

All headings use `text-scout-navy font-display font-black`. Body text uses default `font-body text-gray-700`.

**Create `src/pages/events.astro`** (EVNT-01):

Import BaseLayout. Title: "Events & Term Program".

Page specification:
1. **Hero** — `bg-scout-blue text-white py-16 md:py-20` with heading "Events & Term Program"
2. **Term Program** section — `bg-white py-16`:
   - Data in Astro frontmatter as a `termProgram` array. Use this EXACT structure (matches future Sveltia CMS content collection schema for seamless Phase 4 migration):
   ```javascript
   const termProgram = [
     {
       term: 'Term 1, 2026',
       dates: '27 January - 27 March 2026',
       entries: [
         { date: 'Week 1', activity: 'Welcome back and term planning', sections: 'All sections' },
         { date: 'Week 3', activity: 'Bushcraft skills — fire safety and shelter building', sections: 'Cubs, Scouts' },
         { date: 'Week 5', activity: 'Community service day — Cooroy town clean-up', sections: 'All sections' },
         { date: 'Week 7', activity: 'Night navigation challenge', sections: 'Scouts, Venturers' },
         { date: 'Week 9', activity: 'Family camp weekend at Cooroora', sections: 'All sections' },
       ],
     },
   ];
   ```
   - IMPORTANT (EVNT-01): The success criterion says "at least one real term entry — not placeholder content." The entries above are realistic scout activities for a Queensland group. The group leader should verify/replace with actual term dates, but this is real programmatic content (not lorem ipsum). Flag in the summary that exact dates need group leader confirmation.
   - Render each term as a section with the term name as heading and a table or card list of entries
   - Each entry shows: date/week, activity description, which sections participate
   - Use a clean table with `border-collapse` or card layout with alternating `bg-gray-50/bg-white` rows
3. **Upcoming Events callout** — A `bg-scout-gold/10 border-l-4 border-scout-gold p-4 rounded` note saying "Want to know about specific upcoming events? Get in touch and we'll add you to our mailing list." with link to /join
4. **CTA** — "Questions about our program?" with link to /join

Note: The term data structure uses `entries[].sections` (string) rather than `entries[].section` (enum) to allow "All sections" and multi-section entries. This maps cleanly to a CMS text field in Phase 4.
  </action>
  <verify>
    <automated>cd /c/Users/DCampbell/Source/repos/Cooroy-scouts-website && npm run build 2>&1 | tail -5 && ls src/pages/about.astro src/pages/events.astro 2>&1</automated>
  </verify>
  <done>About page exists at /about with group background, scouting explanation, values grid, and join CTA (ABUT-01). Events page exists at /events with at least one term program containing realistic activity entries in the CMS-compatible data structure (EVNT-01). Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Create Contact/Join page with Web3Forms and hCaptcha</name>
  <files>src/pages/join.astro</files>
  <action>
Create `src/pages/join.astro` — the contact/inquiry form page at /join.

This page fulfills CONT-01 (form with name, child's age, email), CONT-02 (Web3Forms forwarding), and CONT-03 (hCaptcha spam protection).

Import BaseLayout. Title: "Join Us".

**Page structure:**

1. **Hero** — `bg-scout-blue text-white py-16 md:py-20` with heading "Join Cooroy Scouts" and subtext "Get in touch and we'll help your young person find the right section"

2. **Two-column layout** on md+ (single column mobile):
   - **Left column (form):** The Web3Forms contact form
   - **Right column (info):** Quick info cards showing the 4 sections with age ranges and a "Not sure which section?" reassurance paragraph

3. **Contact form specification:**
   - Form element: `<form id="contact-form" action="https://api.web3forms.com/submit" method="POST">`
   - Hidden fields:
     - `<input type="hidden" name="access_key" value="YOUR_ACCESS_KEY_HERE" />` (placeholder — requires user_setup)
     - `<input type="hidden" name="subject" value="New Scouts Inquiry from Cooroy Scouts Website" />`
     - `<input type="hidden" name="from_name" value="Cooroy Scouts Website" />`
   - Honeypot field: `<input type="checkbox" name="botcheck" class="hidden" style="display: none" />`
   - Visible fields (all with labels, `required` where noted):
     - **Your Name** (required): `<input type="text" name="name" required />`
     - **Email Address** (required): `<input type="email" name="email" required />`
     - **Child's Age** (required): `<input type="number" name="child_age" min="4" max="25" required />`
     - **Message** (optional): `<textarea name="message" rows="4"></textarea>`
   - hCaptcha widget: `<div class="h-captcha" data-captcha="true"></div>`
   - Submit button: `<button type="submit" id="submit-btn">` with `bg-scout-gold text-scout-navy font-display font-bold px-8 py-4 rounded-full hover:bg-scout-gold-dark w-full`
   - Result message div: `<div id="result" class="hidden mb-4 p-4 rounded-lg text-sm font-semibold"></div>` above the form

   - Input styling: `w-full border border-gray-300 rounded-lg px-4 py-3 text-sm focus:outline-none focus:ring-2 focus:ring-scout-blue`
   - Label styling: `block font-semibold text-sm text-gray-700 mb-1`

4. **Web3Forms client script** — Add BEFORE closing `</BaseLayout>` but AFTER the template:
   ```html
   <script src="https://web3forms.com/client/script.js" async defer></script>
   ```

5. **Form submission handler** — Add a `<script>` block (client-side, NOT in frontmatter):
   ```typescript
   const form = document.getElementById('contact-form') as HTMLFormElement;
   const result = document.getElementById('result') as HTMLDivElement;
   const submitBtn = document.getElementById('submit-btn') as HTMLButtonElement;

   form?.addEventListener('submit', async (e) => {
     e.preventDefault();
     submitBtn.disabled = true;
     submitBtn.textContent = 'Sending...';
     result.className = 'mb-4 p-4 rounded-lg text-sm font-semibold';
     result.textContent = 'Please wait...';
     result.classList.remove('hidden');

     const formData = new FormData(form);
     const object = Object.fromEntries(formData.entries());
     const json = JSON.stringify(object);

     try {
       const response = await fetch('https://api.web3forms.com/submit', {
         method: 'POST',
         headers: { 'Content-Type': 'application/json', 'Accept': 'application/json' },
         body: json,
       });
       const data = await response.json();

       if (data.success) {
         result.classList.add('bg-green-100', 'text-green-800');
         result.textContent = "Thank you! We'll be in touch soon.";
         form.reset();
       } else {
         result.classList.add('bg-red-100', 'text-red-800');
         result.textContent = data.message ?? 'Something went wrong. Please try again.';
       }
     } catch {
       result.classList.add('bg-red-100', 'text-red-800');
       result.textContent = 'Network error. Please try again.';
     } finally {
       submitBtn.disabled = false;
       submitBtn.textContent = 'Send Inquiry';
       setTimeout(() => result.classList.add('hidden'), 6000);
     }
   });
   ```

**IMPORTANT notes:**
- The access key placeholder `YOUR_ACCESS_KEY_HERE` must be replaced with a real Web3Forms key (see user_setup). The form will NOT send emails until a real key is configured.
- The `<script src="https://web3forms.com/client/script.js">` tag handles hCaptcha widget rendering automatically when a `<div class="h-captcha" data-captcha="true">` exists. No separate hCaptcha account or site key is needed on Web3Forms free plan.
- hCaptcha must ALSO be enabled in the Web3Forms dashboard (see user_setup) — the HTML div alone is not sufficient.
- The form page MUST be at `/join` (not `/contact`) — the Nav CTA already links to `/join`.
- Use `e.preventDefault()` + fetch for submission (not native form POST) to handle success/error states in the UI without page navigation.
  </action>
  <verify>
    <automated>cd /c/Users/DCampbell/Source/repos/Cooroy-scouts-website && npm run build 2>&1 | tail -5 && grep -c "web3forms\|h-captcha\|child_age\|access_key" src/pages/join.astro</automated>
  </verify>
  <done>Join page exists at /join with form containing name, email, and child's age fields (CONT-01). Form POSTs to Web3Forms API with access_key placeholder (CONT-02 — functional once real key is set). hCaptcha div and Web3Forms client script are present (CONT-03 — functional once hCaptcha enabled in dashboard). Build succeeds.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `npm run build` exits 0
2. `ls src/pages/about.astro src/pages/events.astro src/pages/join.astro` — all 3 files exist
3. `grep "termProgram" src/pages/events.astro` — confirms term data structure exists
4. `grep "web3forms" src/pages/join.astro` — confirms Web3Forms integration
5. `grep "h-captcha" src/pages/join.astro` — confirms hCaptcha widget
6. `grep "child_age" src/pages/join.astro` — confirms child's age field (CONT-01)
7. `grep "access_key" src/pages/join.astro` — confirms API key field present (CONT-02)
</verification>

<success_criteria>
- /about page shows group background, scouting explanation, values, and join CTA (ABUT-01)
- /events page shows at least one term with realistic activity entries in CMS-compatible data structure (EVNT-01)
- /join page has form with name, email, child's age fields (CONT-01)
- Form configured with Web3Forms API endpoint and access_key field (CONT-02 — operational once real key is set via user_setup)
- Form includes hCaptcha div and Web3Forms client script (CONT-03 — operational once enabled in Web3Forms dashboard)
- All pages inherit BaseLayout for consistent Nav + Footer
- `npm run build` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-pages/02-03-SUMMARY.md`
</output>
