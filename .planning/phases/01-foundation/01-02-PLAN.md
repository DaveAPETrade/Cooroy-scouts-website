---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - .gitignore
autonomous: false
requirements: [INFR-03]
user_setup:
  - service: cloudflare-pages
    why: "CI/CD deployment pipeline — site must be live on Cloudflare Pages with auto-deploy from GitHub"
    env_vars: []
    dashboard_config:
      - task: "Connect GitHub repository to Cloudflare Pages"
        location: "Cloudflare Dashboard -> Workers & Pages -> Create -> Pages -> Connect to Git"
        details: "Select repository DaveAPETrade/Cooroy-scouts-website, set framework preset to Astro, build command 'npm run build', output directory 'dist', production branch 'main'"

must_haves:
  truths:
    - "The site is live and accessible via HTTPS on a *.pages.dev URL"
    - "A push to the GitHub main branch triggers an automatic Cloudflare Pages rebuild"
    - "The deployed site renders the same Nav, hero, and Footer as local dev"
  artifacts:
    - path: ".gitignore"
      provides: "Git ignore rules for Astro project"
      contains: "node_modules"
  key_links:
    - from: "GitHub main branch"
      to: "Cloudflare Pages"
      via: "Git-connected CI/CD"
      pattern: "push triggers build"
    - from: "Cloudflare Pages build"
      to: "dist/"
      via: "npm run build output"
      pattern: "Astro static output"
---

<objective>
Push the Astro project to GitHub and deploy to Cloudflare Pages with CI/CD, verifying the live site loads over HTTPS and auto-rebuilds on push.

Purpose: This completes the foundation — every future commit to main automatically deploys. Without CI/CD, the team would need manual deployments for every change in Phases 2-4.

Output: A live site at *.pages.dev with automatic deployment from GitHub pushes.
</objective>

<execution_context>
@C:/Users/DCampbell/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/DCampbell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output — executor needs to know the project builds correctly -->
Build command: npm run build
Build output: dist/
Production branch: main
GitHub remote: https://github.com/DaveAPETrade/Cooroy-scouts-website.git
Framework: Astro (static, no adapter needed)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prepare repository for deployment and push to GitHub</name>
  <files>.gitignore</files>
  <action>
1. Ensure `.gitignore` exists and includes standard Astro ignores:
   ```
   # Dependencies
   node_modules/

   # Astro build output
   dist/

   # Environment variables
   .env
   .env.*

   # OS files
   .DS_Store
   Thumbs.db

   # IDE
   .vscode/
   .idea/
   ```
   If a `.gitignore` already exists from the Astro scaffold, verify it includes `node_modules/` and `dist/` at minimum. Add any missing entries.

2. Verify the project builds cleanly before pushing: run `npm run build` — must exit 0.

3. Stage all project files (but NOT `.env` or `node_modules/`), create a commit with message "feat(01-foundation): scaffold Astro project with Tailwind v4 and BaseLayout", and push to the GitHub remote origin on the `main` branch.
   - The remote is already configured: `https://github.com/DaveAPETrade/Cooroy-scouts-website.git`
   - If git remote is not set, add it: `git remote add origin https://github.com/DaveAPETrade/Cooroy-scouts-website.git`
   - Push: `git push -u origin main`

4. Verify the push succeeded by checking `git log --oneline -1` shows the commit on main.
  </action>
  <verify>
    <automated>cd "C:/Users/DCampbell/Source/repos/Cooroy-scouts-website" && git log --oneline -1 && npm run build</automated>
  </verify>
  <done>All project files are committed and pushed to GitHub. `.gitignore` excludes node_modules, dist, and .env files. `npm run build` succeeds. The latest commit is on the main branch at the remote.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Connect Cloudflare Pages to GitHub repository</name>
  <what-built>The Astro project is pushed to GitHub and ready for Cloudflare Pages connection.</what-built>
  <action>
  Claude cannot create Cloudflare Pages projects via CLI without prior authentication. The user must connect the repository in the Cloudflare dashboard.
  </action>
  <how-to-verify>
  **Steps to connect Cloudflare Pages:**

  1. Go to https://dash.cloudflare.com
  2. Navigate to **Workers & Pages** in the left sidebar
  3. Click **Create** -> **Pages** -> **Connect to Git**
  4. Select the GitHub repository: **DaveAPETrade/Cooroy-scouts-website**
  5. Configure build settings:
     - **Framework preset:** Astro
     - **Build command:** `npm run build`
     - **Build output directory:** `dist`
     - **Production branch:** `main`
  6. Click **Save and Deploy**
  7. Wait for the first build to complete (should take ~1-2 minutes)
  8. Copy the *.pages.dev URL that Cloudflare assigns

  **Once deployed, provide:**
  - The *.pages.dev URL (e.g., `cooroy-scouts-website.pages.dev`)
  - Confirmation the build succeeded in the Cloudflare dashboard

  **Note:** No `NODE_VERSION` environment variable is needed — Cloudflare Pages v3 build image defaults to Node.js 22.
  </how-to-verify>
  <resume-signal>Paste the *.pages.dev URL and confirm "deployed" — or describe any build errors</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Verify live deployment and CI/CD pipeline</name>
  <files></files>
  <action>
1. Using the *.pages.dev URL provided by the user in Task 2, verify the site is accessible:
   - Run `curl -s -o /dev/null -w "%{http_code}" https://{project}.pages.dev` — expect HTTP 200
   - Run `curl -s https://{project}.pages.dev | head -50` — verify the HTML contains "Cooroy Scout" in the title and the Nav/Footer markup

2. Verify CI/CD by making a trivial change and pushing:
   - Add a comment to `src/pages/index.astro` (e.g., `<!-- CI/CD verification -->`)
   - Commit with message "chore: verify CI/CD pipeline" and push to main
   - Wait ~2 minutes for Cloudflare to rebuild
   - Re-fetch the page and confirm the comment appears in the HTML source

3. If the deployment URL does not respond or returns errors, diagnose:
   - Check if the build command/output directory in Cloudflare matches (`npm run build` / `dist`)
   - Check if Node.js version is sufficient (should be 22 by default)
   - Report findings to user
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" https://PAGES_DEV_URL_HERE</automated>
  </verify>
  <done>Live site returns HTTP 200 over HTTPS. HTML source contains "Cooroy Scout" title, Nav, and Footer markup. A push to main triggers a new Cloudflare Pages build (verified by the CI/CD test commit). The deployment pipeline is fully operational.</done>
</task>

</tasks>

<verification>
1. Site is live at the *.pages.dev URL over HTTPS
2. `curl` returns HTTP 200 from the live URL
3. HTML source contains the BaseLayout structure (Nav, main content, Footer)
4. A push to GitHub main triggers an automatic Cloudflare Pages rebuild
5. The rebuilt site reflects the latest commit's changes
</verification>

<success_criteria>
- Site deployed and accessible via HTTPS on *.pages.dev
- CI/CD pipeline active: push to main -> automatic Cloudflare Pages rebuild -> updated live site
- No build errors in Cloudflare Pages dashboard
- Deployed site renders the same Nav, hero, section cards, and Footer as local dev
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
