---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - astro.config.mjs
  - package.json
  - tsconfig.json
  - src/styles/global.css
  - src/layouts/BaseLayout.astro
  - src/components/Nav.astro
  - src/components/Footer.astro
  - src/pages/index.astro
  - public/favicon.svg
autonomous: true
requirements: [INFR-01, INFR-02]

must_haves:
  truths:
    - "Astro project builds successfully with zero errors via npm run build"
    - "Tailwind v4 brand tokens (scout-green, scout-gold, font-display, font-body) produce working utility classes"
    - "BaseLayout renders Nav and Footer on every page"
    - "Nav shows horizontal links on desktop (md+) and hamburger menu on mobile"
    - "Join Us button is visually distinct (gold background) and always visible in nav"
    - "Footer contains contact email, Scouts QLD link, registration link, and Facebook link"
    - "Site is responsive at 375px (mobile), 768px (tablet), and 1280px (desktop)"
  artifacts:
    - path: "astro.config.mjs"
      provides: "Astro config with @tailwindcss/vite plugin"
      contains: "tailwindcss"
    - path: "src/styles/global.css"
      provides: "Tailwind v4 brand tokens"
      contains: "@theme"
    - path: "src/layouts/BaseLayout.astro"
      provides: "Root layout with Nav, Footer, slot"
      contains: "slot"
    - path: "src/components/Nav.astro"
      provides: "Responsive navigation with mobile hamburger"
      contains: "menu-toggle"
    - path: "src/components/Footer.astro"
      provides: "Footer with contact and links"
      contains: "scoutsqld"
    - path: "src/pages/index.astro"
      provides: "Landing page using BaseLayout"
      contains: "BaseLayout"
  key_links:
    - from: "src/layouts/BaseLayout.astro"
      to: "src/styles/global.css"
      via: "CSS import"
      pattern: "import.*global\\.css"
    - from: "src/layouts/BaseLayout.astro"
      to: "src/components/Nav.astro"
      via: "component import"
      pattern: "import Nav"
    - from: "src/layouts/BaseLayout.astro"
      to: "src/components/Footer.astro"
      via: "component import"
      pattern: "import Footer"
    - from: "src/pages/index.astro"
      to: "src/layouts/BaseLayout.astro"
      via: "layout wrapper"
      pattern: "import BaseLayout"
    - from: "astro.config.mjs"
      to: "tailwindcss"
      via: "vite plugin"
      pattern: "tailwindcss\\(\\)"
---

<objective>
Scaffold a greenfield Astro 5 project with Tailwind CSS v4, define Scouts QLD brand design tokens, and build a responsive BaseLayout with Nav and Footer components.

Purpose: This is the shell that every subsequent page will inherit. Getting the design system, brand tokens, and responsive layout correct now means every page built in Phase 2+ starts with correct branding and responsive behavior.

Output: A buildable Astro project with working Tailwind v4 integration, brand color/typography tokens, responsive Nav (with mobile hamburger), Footer, and a placeholder index page demonstrating the full layout.
</objective>

<execution_context>
@C:/Users/DCampbell/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/DCampbell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Astro project and configure Tailwind v4 with brand design tokens</name>
  <files>astro.config.mjs, package.json, tsconfig.json, src/styles/global.css, public/favicon.svg</files>
  <action>
1. Initialize the Astro project in the current repo root. Since this is an existing git repo with planning files, do NOT run `npm create astro@latest` into a subfolder. Instead, scaffold directly:
   - Create `package.json` with Astro 5 and required dependencies
   - Run `npm install astro tailwindcss @tailwindcss/vite`
   - Create `tsconfig.json` extending Astro's strict preset: `{ "extends": "astro/tsconfigs/strict" }`

2. Create `astro.config.mjs` with the Tailwind v4 Vite plugin. Do NOT use `@astrojs/tailwind` (deprecated). Do NOT add a Cloudflare adapter (static site, no SSR needed):
   ```javascript
   import { defineConfig } from 'astro/config';
   import tailwindcss from '@tailwindcss/vite';
   export default defineConfig({
     vite: { plugins: [tailwindcss()] },
   });
   ```

3. Create `src/styles/global.css` with this exact structure (order matters):
   - FIRST: Google Fonts `@import url(...)` for Montserrat (700,800,900) and Nunito (400,600,700,800) with `display=swap`
   - SECOND: `@import "tailwindcss";`
   - THIRD: `@theme { }` block containing:
     - `--color-scout-green: #077840;` (Scouts Australia primary green — community-verified, pending official PDF confirmation)
     - `--color-scout-green-dark: #055c30;` (hover state)
     - `--color-scout-gold: #F0CB37;` (Scouts Australia gold)
     - `--color-scout-gold-dark: #d4a800;` (hover state)
     - `--font-display: "Montserrat", "Arial Black", sans-serif;` (headings)
     - `--font-body: "Nunito", "Arial", sans-serif;` (body text)
   - DO NOT place tokens outside `@theme {}` — they won't become Tailwind utility classes in v4

4. Create a simple `public/favicon.svg` — a green fleur-de-lis silhouette or a simple scout-themed SVG icon using the scout-green color (#077840).

5. Verify the scaffold builds: `npm run build` must exit with code 0.
  </action>
  <verify>
    <automated>cd "C:/Users/DCampbell/Source/repos/Cooroy-scouts-website" && npm run build</automated>
  </verify>
  <done>Astro project builds without errors. `global.css` contains `@theme` block with all 4 brand colors and 2 font families. `astro.config.mjs` uses `@tailwindcss/vite` (not `@astrojs/tailwind`). Google Fonts import appears before `@import "tailwindcss"` in global.css.</done>
</task>

<task type="auto">
  <name>Task 2: Build responsive BaseLayout, Nav, and Footer components with placeholder index page</name>
  <files>src/layouts/BaseLayout.astro, src/components/Nav.astro, src/components/Footer.astro, src/pages/index.astro</files>
  <action>
1. Create `src/components/Nav.astro` — responsive navigation:
   - Desktop (md+ breakpoint): horizontal nav links displayed as a flex row. Links: Home (/), Sections (/sections), About (/about), Events (/events), Gallery (/gallery). "Join Us" as a visually distinct CTA button (gold background `bg-scout-gold`, dark text `text-scout-green-dark`, bold, rounded-full, hover state with `bg-scout-gold-dark`).
   - Mobile (below md): hamburger button (three horizontal bars using spans) toggles a slide-down mobile menu. Menu contains all the same links plus "Join Us" CTA.
   - Sticky header: `sticky top-0 z-50` with scout-green background (`bg-scout-green`) and white text.
   - Wordmark: "Cooroy Scouts" text (no logo image — per research, logo usage needs permission). Use `font-display font-black text-xl`.
   - Mobile toggle: minimal inline `<script>` that toggles `hidden` class on the mobile menu div. No Alpine.js or framework needed.
   - Accessibility: hamburger button must have `aria-label="Toggle navigation menu"` and `aria-expanded` toggled by the script.

2. Create `src/components/Footer.astro`:
   - Scout-green background with white/light text
   - Content organized in a responsive grid (1 col on mobile, 3 cols on md+):
     - Column 1: "Cooroy Scout Group" wordmark/heading, brief tagline ("Adventure starts here" or similar), address area (placeholder for now)
     - Column 2: Quick links — Home, Sections, About, Events, Gallery, Join Us
     - Column 3: Connect — Email link (placeholder: info@coorooyscouts.example), link to official Scouts QLD site (https://scoutsqld.com.au), link to Scouts QLD registration (https://scoutsqld.com.au/join/), Facebook page link (placeholder URL)
   - Bottom bar: copyright line "2026 Cooroy Scout Group" and "Part of Scouts Queensland" text
   - Use appropriate spacing (py-12 for main footer, py-4 for bottom bar)

3. Create `src/layouts/BaseLayout.astro`:
   - Typed Props interface: `{ title: string; description?: string }`
   - Default description: "Cooroy Scout Group -- adventure, friendship, and skills for young people aged 5-18 in Cooroy, Queensland"
   - `<head>`: charset UTF-8, viewport meta, description meta, title formatted as "{title} | Cooroy Scout Group", favicon link
   - `<body>`: apply `font-body` class (Nunito), neutral background (`bg-white text-gray-900`)
   - Import and render: `<Nav />`, `<main><slot /></main>`, `<Footer />`
   - Import `../styles/global.css` in the frontmatter to load Tailwind and brand tokens

4. Create `src/pages/index.astro` — placeholder landing page:
   - Wrap in `<BaseLayout title="Home">`
   - Hero section: full-width scout-green background, large heading "Welcome to Cooroy Scouts" in font-display, subheading "Adventure, friendship, and skills for young people aged 5-18" in white, a gold "Join Us" CTA button
   - Brief content section below hero: 2-3 paragraphs of placeholder text about the group, using proper Tailwind typography classes (max-w-4xl mx-auto, responsive padding)
   - Section preview cards area: 4 cards in a responsive grid (1 col mobile, 2 col md, 4 col lg) showing Joeys, Cubs, Scouts, Venturers with placeholder text — demonstrates the design system works across components
   - This page is a PLACEHOLDER to validate the design system. Real content comes in Phase 2.

5. Verify: `npm run build` succeeds and `npm run dev` renders the page with Nav, hero, content, cards, and Footer visible.
  </action>
  <verify>
    <automated>cd "C:/Users/DCampbell/Source/repos/Cooroy-scouts-website" && npm run build</automated>
  </verify>
  <done>BaseLayout renders Nav at top and Footer at bottom. Nav shows horizontal links on desktop (md+) and hamburger menu on mobile. "Join Us" button is gold and visually distinct in both desktop and mobile nav. Footer shows 3-column layout on desktop, stacked on mobile, with Scouts QLD links. Index page renders hero + content + section cards. All text uses Nunito (body) and Montserrat (headings). Brand colors (scout-green, scout-gold) are applied throughout. Build succeeds with zero errors.</done>
</task>

</tasks>

<verification>
1. `npm run build` exits with code 0 — no TypeScript or Astro compilation errors
2. `npm run dev` — open browser at localhost:4321:
   - Nav is visible at top with "Cooroy Scouts" wordmark, nav links, and gold "Join Us" button
   - Resize to 375px width: hamburger menu appears, clicking it reveals mobile nav
   - Resize to 1280px width: horizontal nav links visible
   - Footer is visible at bottom with 3 columns on desktop, stacked on mobile
   - Hero section shows scout-green background with white text and gold CTA
   - Section cards render in responsive grid
   - Inspect element: `font-family` shows Nunito on body, Montserrat on headings
   - Inspect element: computed color on nav background matches #077840
</verification>

<success_criteria>
- Astro 5 project scaffolded with Tailwind v4 via `@tailwindcss/vite` (NOT `@astrojs/tailwind`)
- Brand tokens defined in `@theme {}` block: scout-green (#077840), scout-gold (#F0CB37), Montserrat (display), Nunito (body)
- BaseLayout wraps all pages with Nav + Footer via slot
- Nav is responsive: horizontal on md+, hamburger on mobile, "Join Us" gold CTA always visible
- Footer includes Scouts QLD links, contact placeholder, social link
- Site builds cleanly with `npm run build`
- Index page demonstrates full design system (colors, typography, responsive grid)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
